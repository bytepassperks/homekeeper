═══════════════════════════════════════════════════════════════════
                     COPY & PASTE THESE EXACTLY
═══════════════════════════════════════════════════════════════════

STEP 1: Open VS Code
STEP 2: Press Ctrl + Shift + H
STEP 3: Click the .* button (enable regex)

STEP 4: In "Find" box, copy and paste this:

@([0-9]+\.){2}[0-9]+"

STEP 5: In "Replace" box, copy and paste this:

"

STEP 6: Click "Replace All"
STEP 7: Press Ctrl + K, then S (save all files)

STEP 8: Open Git Bash and run:

cd ~/Documents/homekeeper
git add .
git commit -m "Fix: Remove versioned imports"
git push origin main

STEP 9: Wait 5 minutes for Render to deploy

STEP 10: SUCCESS! Your website is LIVE! 🎉

═══════════════════════════════════════════════════════════════════

FIND PATTERN (copy this):
@([0-9]+\.){2}[0-9]+"

REPLACE PATTERN (copy this):
"

═══════════════════════════════════════════════════════════════════

DO IT NOW! 2 MINUTES TO SUCCESS!
